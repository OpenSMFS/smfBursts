# include <stdlib.h>
# include <stdint.h>
# include <math.h>

int64_t simulate_photon_trajectory(int64_t (*rng)(), int64_t size, int64_t ndet, int64_t npois, double *lambdas){
	/* rng: function for random number generator
	 * size: size of output trajectory
	 * ndet: number of detector indexes
	 * npois: number of poisson processes
	 * lambas: array ndet x npois in size of expectation value for each poisson process
	*/ 
}
